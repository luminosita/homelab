apiVersion: apps/v1
kind: Deployment
metadata:
  name: netbird-backend-management
spec:
  template:
    spec:
      dnsConfig:
        nameservers:
          - 192.168.50.253 # AdGuardHome
      dnsPolicy: None
